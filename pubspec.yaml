name: app_barba
description: Aplicativo web para gerenciamento de barbearia
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.9

  # Estado simples
  provider: ^6.1.5

  # Banco e autenticação
  supabase_flutter: ^2.15.0

  # Datas
  intl: ^0.20.2

  # Fontes
  google_fonts: ^8.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/